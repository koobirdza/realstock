import { STORAGE_KEYS } from "./config.js";import { setEmployee, state } from "./state.js";export function saveSession(){localStorage.setItem(STORAGE_KEYS.session, JSON.stringify({ employee: state.employee }));}export function restoreSession(){try{const raw=localStorage.getItem(STORAGE_KEYS.session);if(!raw)return;const obj=JSON.parse(raw);if(obj?.employee)setEmployee(obj.employee);}catch(err){}}export function clearSession(){localStorage.removeItem(STORAGE_KEYS.session);}