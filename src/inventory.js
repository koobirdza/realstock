import { MAX_QTY } from "./config.js";
import { state } from "./state.js";
import { getItems, getNodeByPath, needsDestination } from "./catalog.js";
import { nowIso, todayIso } from "./utils.js";
export function collectRecords(catalogTree,inputRows){ if(!state.employee) throw new Error("ยังไม่ได้เข้าสู่ระบบ"); if(!state.mode) throw new Error("ยังไม่ได้เลือกโหมด"); if(state.mode==="order") throw new Error("โหมดสั่งของเป็นโหมดดูข้อมูล ไม่ต้องบันทึก"); if(needsDestination(state.mode,state.path)&&!state.destination) throw new Error("กรุณาเลือกปลายทางการเบิกก่อนบันทึก"); const node=getNodeByPath(catalogTree,state.path); if(!node) throw new Error("ไม่พบหมวดรายการปัจจุบัน"); const items=getItems(node); const chosen=inputRows.filter(x=>x.value!=="").map((row)=>{ const item=items[row.index]; if(!item) return null; const qty=Number(row.value); if(Number.isNaN(qty)||qty<0||qty>MAX_QTY) throw new Error(`จำนวนไม่ถูกต้อง: ${item.item_name}`); if((state.mode==="issue"||state.mode==="receive")&&qty===0) return null; return { timestamp: nowIso(), date: todayIso(), employee: state.employee, action: state.mode, category: item.target_category||state.path[0]||"", main_category: item.main_category||"", sub_category: item.sub_category||"", item: item.item_name, item_name: item.item_name, brand: item.brand||"-", qty, unit: item.unit||"", note:"", item_key:item.item_key, item_type:item.item_type||"", from_category: state.mode==="issue" ? "stock" : "", to_category: state.mode==="issue" ? (state.destination||"") : (item.target_category||state.path[0]||"") }; }).filter(Boolean); if(!chosen.length) throw new Error(state.mode==="count"?"กรุณากรอกยอดคงเหลืออย่างน้อย 1 รายการ":state.mode==="issue"?"กรุณากรอกจำนวนที่เบิกอย่างน้อย 1 รายการ":"กรุณากรอกจำนวนที่รับเข้าอย่างน้อย 1 รายการ"); return chosen; }
