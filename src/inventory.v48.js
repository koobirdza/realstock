import { state } from './state.v48.js';
export function collectRecords(items=[]){const rows=[];for(const item of items){const raw=state.pendingValues[item.item_key];const qty=Number(raw);if(raw===''||raw===undefined||raw===null||!Number.isFinite(qty)||qty<0)continue;if(qty===0&&state.mode!=='count')continue;rows.push({item_key:item.item_key,item_name:item.item_name,brand:item.brand,unit:item.unit,item_type:item.item_type,target_category:item.target_category,main_category:item.main_category,sub_category:item.sub_category,qty,employee:state.employee,action:state.mode,note:''});}return rows;}
